/* 
string function provides a list of functions 

*/

// var str = 'Brijesh kumar pandey';
// var res = str.toUpperCase(str);
// console.log(res);

// var str = 'BRIJESH';
// var res = str.toLowerCase(str);
// console.log(res);

var str = 'BRIJESH';
var res = str.at(index);
var res = str.indexOf(searchString);
var res = str.concat();
var res = str.includes(searchString);
var res = str.length();
var res = str.repeat(count);
var res = str.replace(searchValue, replaceValue);
var res = str.slice();
var res = str.split(separator);
var res = str.trim();
var res = str.trimEnd();
var res = str.trimStart();
