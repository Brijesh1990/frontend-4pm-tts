/*  
what is array in javascript  ?
   array is a collection of multiple data in a single elements there we used array 

   or 

   array is stored multiple data in a single elements there we used array 

   var arr=Array(1,2,3,4,5,6);
   console.log(arr);


*/

// array as literals
var emp = ['kaushik', 'jay', 'bhumika', 'ramesh'];
console.log(emp);

// array as constructor
var arr = Array(1, 2, 3, 4, 5, 6);
console.log(arr);

// array as instance
var arr = new Array(1, 2, 3, 4, 5, 6);
console.log(arr);
